<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="dinero.RegisterPage">
    <ContentPage.Content>
        <StackLayout>
            <Label>Enter your name:</Label>
            <Entry x:Name="txtName"/>
            <Label Text="{Binding errorName}" IsVisible="{Binding isNameValid}"TextColor="DarkRed"/>
            <Entry x:Name="txtEmail"/>
            <Label Text="{Binding errorEmail}" IsVisible="{Binding isEmailValid}"TextColor="DarkRed"/>
            <Entry x:Name="txtPassword" IsPassword="true"/>
            <Label Text="{Binding errorPassword}" IsVisible="{Binding isPasswordValid}" TextColor="DarkRed"/>
            <Button x:Name="btnRegister" Text="Register"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>